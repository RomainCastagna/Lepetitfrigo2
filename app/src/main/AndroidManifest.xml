<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exercices.lepetitfrigo">

    <uses-feature
        android:name="android.hardware.Camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CategorieActivity"></activity>
        <activity android:name=".QRScannerActivity" />
        <activity
            android:name=".ProductShopActivity"
            android:label="@string/title_activity_product_shop"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Inscription3" />
        <activity android:name=".Inscription2" />
        <activity android:name=".Inscription1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>